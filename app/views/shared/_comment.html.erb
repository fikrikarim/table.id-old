<div class="comment-container">

  <%= render 'shared/votebutton', source: comment %>

  <div class="comment-text-container">
    <%= comment.text %>
    <%= comment.user.username %>
    <%= time_ago_in_words(comment.created_at) %>
  </div>
</div>
